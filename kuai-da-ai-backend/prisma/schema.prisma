// prisma/schema.prisma

// 1. 定义Prisma客户端生成器
generator client {
  provider = "prisma-client-js"
}

// 2. 定义数据源 (这是你之前缺失的部分)
datasource db {
  provider = "postgresql" // 告诉Prisma我们用的是PostgreSQL
  url      = env("DATABASE_URL") // 告诉Prisma去 .env 文件里找 DATABASE_URL
}

// 3. 定义用户数据模型
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
}